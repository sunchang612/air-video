import { } from '@tarojs/taro'
import { View, Button, Text, Image } from '@tarojs/components'

import './index.scss'

export default function CardItem(props) {
  console.log('props--->', props)
  const { item } = props
  return (
    <View className="card-item-wrapper">
    
    </View>
  )
}